name: Test armv7 architecture
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          arch: armv7
          distro: ubuntu20.04
          run: |
            uname -m
            echo "arch_output=$(uname -m)" >> $GITHUB_OUTPUT
      - name: Assert armv7 architecture or graceful failure
        env:
          ARCH: ${{ steps.action.outputs.arch_output }}
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          echo "arch: $ARCH"
          if [ "$OUTCOME" = "success" ]; then
            if [ "$ARCH" != "armv7l" ]; then
              echo "FAIL: expected armv7l but got '$ARCH'"
              exit 1
            fi
            echo "PASS: architecture is armv7l"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (QEMU/Docker infrastructure not available)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
